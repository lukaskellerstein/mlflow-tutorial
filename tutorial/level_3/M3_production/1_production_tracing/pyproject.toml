[project]
name = "mlflow-tutorial-L3-M3-production-tracing"
version = "0.1.0"
description = "Production Tracing Strategies — sampling, structured metadata, error tracing, and performance monitoring"
requires-python = ">=3.10"
dependencies = [
    "mlflow>=2.0",
    "langchain-ollama>=0.2",
    "langchain-core>=0.3",
    "pandas>=2.0",
    "ollama>=0.4",
]
