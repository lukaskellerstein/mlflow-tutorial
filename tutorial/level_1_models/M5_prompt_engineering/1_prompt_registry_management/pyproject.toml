[project]
name = "mlflow-tutorial-L1-M5-1-prompt-registry-management"
version = "0.1.0"
description = "Prompt Registry and Management at Scale"
requires-python = ">=3.10"

[project.dependencies]
mlflow = ">=2.0"
openai = ">=1.0"
pandas = ">=2.0"
