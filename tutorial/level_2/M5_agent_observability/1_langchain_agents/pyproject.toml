[project]
name = "mlflow-tutorial-L2-M5-langchain-agents"
version = "0.1.0"
description = "LangChain Agent Tracking with MLflow"
requires-python = ">=3.10"
dependencies = [
    "mlflow>=2.0",
    "langchain>=0.3",
    "langchain-openai>=0.1",
    "langchain-core>=0.3",
    "langgraph>=0.2",
]
