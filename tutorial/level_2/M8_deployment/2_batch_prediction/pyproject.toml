[project]
name = "mlflow-tutorial-L2-M8-batch-prediction"
version = "0.1.0"
description = "Batch Prediction Pipelines"
requires-python = ">=3.10"
dependencies = [
    "mlflow>=2.0",
    "scikit-learn>=1.0",
    "pandas>=2.0",
]
