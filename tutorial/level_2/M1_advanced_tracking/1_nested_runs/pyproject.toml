[project]
name = "mlflow-tutorial-L2-M1-nested-runs"
version = "0.1.0"
description = "Nested Runs and Run Hierarchies"
requires-python = ">=3.10"
dependencies = [
    "mlflow>=2.0",
    "openai>=1.0",
    "pandas>=2.0",
]
