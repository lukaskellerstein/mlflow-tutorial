[project]
name = "mlflow-tutorial-L2-M1-2-2-deepagents"
version = "0.1.0"
description = "Multi-turn DeepAgents conversations — persistent todos and files across one session"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2",
    "mlflow>=3.15",
    "deepagents>=0.7",
    "langchain>=1.3",
    "langchain-openai>=1.4",
    "langchain-core>=1.5",
    "langgraph>=1.2",
]
