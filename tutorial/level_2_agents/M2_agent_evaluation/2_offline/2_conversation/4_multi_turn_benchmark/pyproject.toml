[project]
name = "mlflow-tutorial-L2-M2-2-2-2-multi-turn-benchmark"
version = "0.1.0"
description = "A Multi-Turn Agent Benchmark: pinned user, state metric, pass^k"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2",
    "mlflow[genai]>=3.15",
    "langchain>=1.0",
    "langchain-openai>=1.0",
    "langchain-core>=1.0",
]
