[project]
name = "mlflow-tutorial-L2-M2-3-2-1-online-session-scoring"
version = "0.1.0"
description = "Online Session Scoring: sampling whole conversations from production"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2",
    "mlflow[genai]>=3.15",
    "langchain>=1.0",
    "langchain-openai>=1.0",
    "langchain-core>=1.0",
]
