[project]
name = "mlflow-tutorial-L2-M2-1-claude-agent-sdk"
version = "0.1.0"
description = "Claude Agent SDK + MLflow — custom tracing integration with external MCP server"
requires-python = ">=3.10"
dependencies = [
    "mlflow>=2.0",
    "claude-agent-sdk>=0.2",
    "anyio>=4.0",
    "mcp[cli]>=1.4",
]
