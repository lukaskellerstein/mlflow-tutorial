[project]
name = "mlflow-tutorial-L2-M2-2-deepagents"
version = "0.1.0"
description = "DeepAgents + MLflow — multi-agent orchestration with auto-tracing"
requires-python = ">=3.11"
dependencies = [
    "mlflow>=2.0",
    "deepagents>=0.6.12",
    "langchain-openai>=0.1",
    "pandas>=2.0",
]
