[project]
name = "mlflow-tutorial-L2-M3-3-benchmark-optimization"
version = "0.1.0"
description = "Optimizing Against Benchmarks Without Destroying Them"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2",
    "mlflow[genai]>=3.0",
    "langchain>=1.0",
    "langchain-openai>=1.0",
    "langchain-core>=1.0",
]
