[project]
name = "mlflow-tutorial-L2-M4-3-custom-benchmark"
version = "0.1.0"
description = "Building a Custom Domain-Specific Agent Benchmark"
requires-python = ">=3.10"

[project.dependencies]
mlflow = ">=2.0"
langchain = ">=1.0"
langchain-openai = ">=0.3"
pandas = ">=2.0"
